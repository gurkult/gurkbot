{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Setup",
            "type": "shell",
            "command": "pipenv sync --dev && pipenv run precommit && pipenv run pre-commit run --all-files",
            "problemMatcher": []
        },
        {
            "label": "Run Bot",
            "type": "shell",
            "command": "docker-compose up",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
